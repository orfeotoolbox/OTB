file(GLOB OTBMUPARSERX_SRCS "*.cpp" )
#list(REMOVE_ITEM OTBMUPARSER_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/muParserComplex.cpp")

add_library(otbmuparserx ${OTBMUPARSERX_SRCS})
if(OTB_LIBRARY_PROPERTIES)
  set_target_properties(otbmuparserx PROPERTIES ${OTB_LIBRARY_PROPERTIES})
endif()

if(NOT OTB_INSTALL_NO_LIBRARIES)
  install(TARGETS otbmuparserx
    RUNTIME DESTINATION ${OTB_INSTALL_BIN_DIR} COMPONENT RuntimeLibraries
    LIBRARY DESTINATION ${OTB_INSTALL_LIB_DIR} COMPONENT RuntimeLibraries
    ARCHIVE DESTINATION ${OTB_INSTALL_LIB_DIR} COMPONENT Development)
endif()

if(NOT OTB_INSTALL_NO_DEVELOPMENT)
  file(GLOB OTBMUPARSERX_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")
  #list(REMOVE_ITEM OTBMUPARSER_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/muParserComplex.h")
  install(FILES ${OTBMUPARSERX_HEADERS}
    DESTINATION ${OTB_INSTALL_INCLUDE_DIR}/Utilities/otbmuparserx
    COMPONENT Development)
endif()
